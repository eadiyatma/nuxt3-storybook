import { Canvas, Meta, Story } from "@storybook/addon-docs";

import MyButton from "../components/MyButton.vue";

<Meta title="MDX/MyButton" component={MyButton} />

export const Template = (args) => ({
  components: { MyButton },
  setup() {
    return { args };
  },
  template: '<MyButton v-bind="args" />',
});

# MyButton

Let's define a story for our `MyButton` component:

<Story
  name="positive"
  args={{
    status: "positive",
    label: "Positive",
  }}
>
  {Template.bind({})}
</Story>

We can drop it in a `Canvas` to get a code snippet:

<Canvas>
  <Story
    name="negative"
    args={{
      label: "Negative",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

We can even preview multiple Stories in a block. This
gets rendered as a group but defines individual stories
with unique URLs, which is great for review and testing.

<Canvas>
  <Story
    name="warning"
    args={{
      status: "warning",
      label: "Warning",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="neutral"
    args={{
      status: "neutral",
      label: "Neutral",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="error"
    args={{
      status: "error",
      label: "Error",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
